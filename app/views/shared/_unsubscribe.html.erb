<%= form_for(current_user.active_relationships.find_by(subscribed_id: @other_user.id),
             html: { method: :delete }) do |f| %>
  <%= f.submit "Unsubscribe", id:"suggestion_subscribe_btn" %>
<% end %>
