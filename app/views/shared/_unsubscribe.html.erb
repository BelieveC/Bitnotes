<%= form_for(current_user.active_relationships.find_by(subscribed_id: session[:category_user_id]),
      html: { method: :delete},remote: true) do |f| %>
  <%= f.submit "Unsubscribe",id:"suggestion_subscribe_btn" %>
<% end %>



