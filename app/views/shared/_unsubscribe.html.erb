<%= form_for(current_user.active_relationships.find_by(subscribed_id: @user.id),
      html: { method: :delete},remote: true) do |f| %>
  <div><%= hidden_field_tag :form_id,  fi %></div>
  <%= f.submit "Unsubscribe",id:"suggestion_subscribe_btn" %>
<% end %>



